run = "pip install -r backend/requirements.txt && cd frontend && npm install && npm run build && cd .. && uvicorn backend.main:app --host 0.0.0.0 --port 8080"

[env]
PORT = "8080"

[nix]
channel = "stable-24_05"

[languages.python]
pattern = "*.py"

[languages.javascript]
pattern = "*.js"
